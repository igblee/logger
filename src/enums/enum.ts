export enum Severity {
  DEBUG = 0,
  INFO = 1,
  WARN = 2,
  ERROR = 3,
  FATAL = 4,
}

export enum LoggerLabel {
  DEBUG = '#LOGGER DEBUG#',
  INFO = '#LOGGER INFO#',
  WARN = '#LOGGER WARN#',
  ERROR = '#LOGGER ERROR#',
  FATAL = '#LOGGER FATAL#',
}

export enum CompressedLoggerLabel {
  DEBUG = '#0#',
  INFO = '#1#',
  WARN = '#2#',
  ERROR = '#3#',
  FATAL = '#4#',
}
